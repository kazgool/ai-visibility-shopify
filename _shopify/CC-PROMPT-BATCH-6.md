Batch 6. The doubt-driven review of batch 5's item 2, item 6 and the ourProductNode mechanism, before any of batch 5 is pushed. Same hard rules as the five earlier prompts in _shopify/. Run scripts/queue-unstick.ts before the push. Nothing pushed until Marius says so.

Standing rules, unchanged from batch 5: no group, source or capability switched off or withdrawn; nothing a person wrote is overwritten; every figure carries its denominator; a rule that runs on real data is derived from the corpus, never written from memory; the 1 percent bar is never loosened, and a check is never silenced to make a run go green. Report progress per item as you go.

## A. Two confirmed defects in what batch 5 already committed

1. app/engine/delimit.ts, rule 1 (BOUND_PATTERN, boundBefore). The pattern has no left word boundary: "Cumin 5g" reads the bound "min" out of the tail of "Cumin", and "Termin 5 minute" the same out of "Termin". Any merchant word ending in a listed bound token (min, max, sub, peste, etc.) can fabricate a bound that was never stated. Fix: require a non-letter, or the start of string, immediately before the matched bound. Add a corpus-derived test for at least these two shapes and one more real product string with this pattern found in the corpus. Re-run the item 6 and item 7 measurement after the fix and report whether the hold-out and Republica BIO numbers move, and by how much.

2. app/engine/delimit.ts, rule 4 (endsOnLooseFigure, FIGURE_WITH_UNIT) and its wiring in extract.ts. The rule discards any multi-word capture ending in a figure-plus-short-suffix token, regardless of whether the capture was actually truncated by the window (rule 5 already tests that, separately, and independently). Two corpus-plausible losses: a legitimate value like "produsul contine 250g" (a net weight stated inside a full sentence, not a truncation) and a size code like "Marime disponibila 5XL" (not a unit at all). Read the corpus for real instances of both shapes across any store's Gramaj, Cantitate pachet and Marime or Format groups, with counts. Fix: only discard when the capture also shows truncation evidence (the window was cut, or the sentence provably continues past the figure), never on the figure-ending shape alone. Report before and after counts on the same measurement as item 1.

## B. Liquid fix, one gap

3. extensions/ai-visibility/blocks/ai-visibility.liquid, the item 2 rating coercion. Confirmed safe against Hash, Array and nil metafield shapes (Shopify's own to_number defaults those to 0, no crash) and against scientific notation (Ruby's to_i does not parse it, "1e10".to_i is 1). One real gap: a Romanian decimal comma, "4,5", is read by Liquid's plus filter as 4, not 4.5 and not 0. This silently publishes a wrong rating instead of suppressing it, the opposite of what the fix's own comment claims ("the safe direction"). Fix: normalize comma to period before plus: 0, the same way the codebase already does for measurements (m[2].replace(",", ".") in delimit.ts). While in the file: av_rating_count has no nested .value.value fallback that av_rating_value has; add it for symmetry. One line, low priority, but note it in the same commit.

## C. The ourProductNode single-page mechanism, before trusting the item 12 deploy-then-rescan plan

4. Read isOurNode() in app/services/theme-scan.server.ts, not read in this round, and confirm or rule out that it correctly recognizes our Product node on a gift card product specifically. card-cadou-republica-bio is a Shopify gift card, a distinct product type and template from a normal product. If gift cards render through a path isOurNode() or the fragment detection does not cover, say so before the deploy-then-rescan plan is trusted to fix that exact page.

5. Reconcile "179 of 182" from the nightly aggregate scan against the 3 specific pages named in item 2's diagnosis: the two Manuka honey products (miere-de-manuka-manuka-lab-mgo-300..., miere-de-manuka-manuka-lab-mgo-525...) and card-cadou-republica-bio. Confirm these are the same 3 pages the aggregate scan is missing, not a different set with a separate cause. One table, page by page, with what each scan saw.

6. FIRST_PRODUCT in app/routes/app.diagnostics.tsx has no explicit sortKey. Checked against Shopify's own docs: the default is ID ascending, which is stable under normal catalogue growth (a new product gets a higher ID and never sorts first) and only changes if the current lowest-ID published product is unpublished or deleted. Add sortKey: ID explicitly to the query so this stops resting on an implicit default that could change if Shopify's own default ever does, and add one sentence to the code comment recording why ID-ascending is being relied on here. No architecture change in this item, that is section D.

7. Between a deploy and Marius clicking "re-run theme scan," the rescan can read a stale, pre-deploy page if Shopify's storefront cache has not yet turned over, recording ourProductNode: false again and looking exactly like the fix failed when it did not. Add a cheap guard if one exists (a build or version marker already available, or comparing against the deploy's own timestamp) and surface a warning on the rescan result when the fetched page looks like it predates the current deploy. If no cheap signal exists, report that plainly rather than inventing one; do not ship a guess.

## D. Decisions Marius still owes, do not decide them yourself, carried forward and new

- Fixture C: amend it so rule 3, merged dimensions, can enable, or drop rule 3. Unchanged from batch 5.
- The abstention setting (0 to 3) for the mechanical rules. Unchanged question from batch 5, Claude Code's own reading was to take none. The numbers behind it move once A1 and A2 above are fixed and the measurement is re-run: do that first, then reprint the abstention table, since both fixes touch groups already inside it.
- What the 1 percent bar is for, given item 10's finding that the source meeting the bar publishes shipping policy 189 times while the two sources that would answer product questions are held back by it. Unchanged from batch 5.
- New, raised by the doubt review: whether ourProductNode should be sourced from the nightly 182-page aggregate scan, which already holds better evidence, instead of one arbitrarily selected "first published product" page. The single-page signal has no reconciliation against the aggregate and no alert when it silently diverges from it.
- New: whether a re-scan should trigger automatically on shopify app deploy, or off the nightly cron, rather than depending on a manual click on the SEO screen with no reminder and no staleness check on the metafield.
- The three items already printed in _shopify/DECISIONS-FOR-MARIUS-2026-09-12.md: the phrase table, the refund wording conflict, the FAQ cap. Unchanged, still waiting.
- New, a policy question, not a bug: rule 6 deliberately allows "contine gluten si lactoza" truncated to "contine gluten" as not-an-error, per the existing rubric's own definition of a true but partial statement. On a food and supplement client, should an allergen statement specifically be held to a stricter bar than the general rubric, given a shopper allergic to milk could read the truncated form as complete and safe.

## E. Close

Docs and handover as before: CHANGELOG per item in house style, STATUS.md updated. Full .\check.bat after every commit, with the test guard. In the handover, one line per item of this prompt: done, with the number, or not done, with the number and the reason. State plainly which of A1, A2 and B3's fixes moved the item 6 or item 7 measurement numbers, and by how much, since three of the decisions in section D above all read numbers these fixes can change.
