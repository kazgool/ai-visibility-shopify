Read, in this order, before touching anything: CLAUDE.md, _shopify/PRD-AI-READABILITY.md (the spec you are implementing, every P0 and P1 item), _shopify/HANDOFF-2026-09-11.md, _shopify/CHANGELOG.md "Unreleased". Then `git status`: the tree already carries uncommitted, untested work from today that you keep and test, not redo: scripts/queue-unstick.ts (SELECT from graphile_worker.jobs), scripts/read-llms-txt.ts (new), app/services/llms-txt.server.ts and its test (blockquote, Products before Optional), extensions/ai-visibility/blocks/ai-visibility.liquid (describedby link moved shop-wide). Ignore the CRLF-only diffs (`git diff -w --stat` shows them empty) and the stray untracked CSV `ibility-shopify` in the root; do not delete it, do not commit it.

Goal: one deploy. Implement everything below as small atomic commits on main, do not push, do not run `shopify app deploy`, do not run `fly deploy`. Marius pushes and deploys once, after your handover.

Hard rules, from CLAUDE.md and the PRD, non-negotiable:
- Liquid blocks ship zero JavaScript, never fetch, never set fonts or colours. Every block renders nothing at all when it has nothing to say: no wrapper, no heading, no empty list.
- Never a second complete Product node from us. Never mark up content that is not visible on the page.
- Nothing a human wrote is overwritten. No new write paths to Shopify in this batch; everything here is rendering, scanning and reporting.
- A change to an exported type, shared constant or signature is not done until every construction site is grepped across app, worker, scripts, extensions and every __tests__ directory; put the hit count per symbol in the handover.
- Every merchant-facing string in English, plain characters only (no em dash, no curly quotes, no ellipsis character, no entities), through cleanOutput where it is data.
- Check counts and finding codes: the SEO test suite hardcodes the number of checks (see seo-report.test.ts and the three tests updated for "32" on 11 September); a new finding code changes it; update the tests deliberately, do not loosen them.
- Every new merchant-visible how-to text is checked against the forbidden jargon list in seo-report.test.ts.
- Before any commit that adds or moves a test file: run the suite once with .env renamed away (CI has no .env).
- Do not silence a check, loosen a threshold or skip a test to go green. If something cannot pass, say so in the handover.

Work items, in this order. Each one is a commit; run the relevant tests before each commit and the full suite before the last.

1. Baseline. Run `.\check.bat`. Paste its last 15 lines into a scratch note; that is the "before". If it is red on the already-present work, fix that first (it was written blind, without a test run) and say what was wrong.

2. Worker signals (handoff open item 3). worker/index.ts: handle SIGTERM and SIGINT by calling the graphile runner's stop() and awaiting it with a 25 s ceiling, then exit 0; log both events. Set maxJobExpiry to 30 minutes in the runner options (default is 4 h) and document why in a comment (a deploy mid-job orphans the lock for the whole expiry). Dashboard guard: wherever the one-job-at-a-time guard reads JobRun rows with status "running", treat a row whose startedAt is older than that same expiry as stale: show it as "stuck, released" rather than "running" and do not let it disable the buttons. Find that guard by grepping JobRun and "running" across app/routes and app/services; list the files you changed. Unit test the stale rule.

3. App embed default (handoff open item 4). In ai-visibility.liquid schema, the "enabled" setting keeps `"default": true` only if you can cite a Shopify doc saying an app embed's setting default activates it on an existing theme; otherwise remove the default and rely on the dashboard's embed step. Record which you did and why in the CHANGELOG entry. Do not change anything else in that schema.

4. theme_scan gains two flags (PRD P0.4 and P0.5). In app/services/theme-scan.server.ts persist `hasProductLd` (theme has at least one Product node that is not ours) and `hasWebSiteLd` (theme has a WebSite node that is not ours) in the theme_scan shop metafield, next to organizationId and productId. Grep every reader of theme_scan (Liquid, services, routes, tests) and list them. Tests for both flags, both values.

5. Extend falls back to full (P0.5). ai-visibility.liquid: in extend mode, hold the Product node back only when `hasProductLd` is true and productId is blank (theme node without @id, B33 case). When `hasProductLd` is false, extend mode emits the complete node exactly as full mode does. Rewrite the block comment at the extend/full decision so it describes the three cases truthfully (theme node with @id: extend it; theme node without @id: hold back, B33; no theme node: full node). Update B33 how-to text if it now reads wrong. Add liquidjs fixture tests in the style of scripts/check-liquid-json.mjs or in vitest with liquidjs for the three cases, asserting the number of Product nodes and the @id used.

6. One WebSite node (P0.4). Emit our WebSite node on the home page only when `hasWebSiteLd` is false. In the conflicts card (app/services/conflicts.ts and its readers), when the duplicate WebSite is ours, say so in plain words instead of "unknown source" and say it disappears after the next scan; when it is the theme's plus ours before the scan has run, say that. Test both.

7. Visible product block (P0.1). New file extensions/ai-visibility/blocks/ai-visibility-content.liquid, app embed, `"target": "body"`, name "AI Visibility content". Renders only on template.name == 'product' with a product. Settings, all checkbox on by default unless noted: heading text (text, default "About this product"), show_summary, show_facts, show_fit, show_questions, show_links (default on), heading_level (select h2 or h3, default h2). Content order: heading; summary as one paragraph; facts as a `<dl>` of label and value pairs exactly as stored in $app.facts (read the JSON shape from app/services/facts.server.ts and the mirror renderer in app/services/mirror.server.ts, reuse the mirror's label order); fit_for as one line "Suits: ..."; questions as `<h3>`/`<h4>` question then `<p>` answer (level one below heading_level); then two lines: `<a href="{{ shop.url }}/apps/ai-visibility/{{ product.handle }}">Plain-text version of this page</a>` and `<a href="{{ shop.url }}/apps/ai-visibility/llms.txt">All products as plain text</a>`. Wrapper `<section class="ai-visibility-content" style="margin:2rem 0;max-width:72rem;">`. Escape every value with Liquid's escape filter. If none of summary, facts, fit_for, questions has content, render nothing (not even the section). When the `manual_placement` setting from item 9 is on, render nothing on product pages. No JavaScript, no external request, no font, no colour.

8. Visible collection block (P0.2). Same pattern in the same file (one embed can branch on template.name) or a second embed file if the schema needs different settings; your call, say which and why. On template.name == 'collection': collection summary, criteria as a list, questions as above, and the comparison table, hidden by the same `manual_placement` setting when the merchant has placed the comparison-table app block. Empty rule identical.

9. Placeable product block (P1.1). New app block extensions/ai-visibility/blocks/product-content.liquid, `"target": "section"`, same content and settings as item 7, so a merchant can put it where they want. Liquid variables do not cross block boundaries and Shopify documents no way for an app embed to detect an app block of the same extension on the page, so do not fake detection. The embed from item 7 gets one more checkbox setting, `manual_placement`, label "I placed the content block myself; hide the automatic one", default off. When on, the embed renders nothing on product pages. Same for collections against the comparison-table block. Document it in SUPPORT.md in two sentences. Share the rendering between item 7 and item 9 through one snippet in extensions/ai-visibility/snippets/ (theme app extensions support snippets rendered with {% render %}); the two blocks pass their settings in as render arguments so the markup exists once.

10. Structured data mirrors visible content (P0.3). Blocks of one extension cannot read each other's settings, so the FAQPage node moves out of the head block and into the body content (items 7 and 9): the same block that prints the visible questions prints the FAQPage `<script type="application/ld+json">` right after them, and only when show_questions is on and there are questions. Remove FAQPage from ai-visibility.liquid entirely and update the comment there that says FAQPage is emitted. The Product node in the head keeps description from the theme's product description (truncated, as the non-summary branch does today) and drops additionalProperty from facts, because the head block cannot know whether the body shows them; the facts reach readers as visible text and as the mirror. Update every test that asserted FAQPage or additionalProperty in the head output. Write in the CHANGELOG entry why: Google's structured data policy, "Don't mark up content that is not visible to readers of the page", https://developers.google.com/search/docs/appearance/structured-data/sd-policies.

11. Visible-text check script (P0.3 acceptance). scripts/read-ld-visible.ts, read only: for a shop domain, fetch N product pages through the app's existing page fetcher (see seo-page.server.ts for the fetch and extractLdNodes), collect every string value in nodes carrying our marker "https://mrdigital.ro/ns/ai-visibility", and report each string absent from the page's visible text (strip tags, decode entities, collapse whitespace). Print counts with denominators and the first 20 misses with product handle and node type. No writes.

12. Page check B34 and the delivery counter (P0.6). New finding code B34 "Visible product content from this app is on the page", source B, counted not judged (no red state), raised when the page carries `class="ai-visibility-content"`. SEO screen and Report screen: a row "Visible on the page: N of M eligible products" from the nightly scan with the existing denominator rules. Update the hardcoded check count in tests deliberately. Merchant text checked against the jargon list.

13. Crawler families (P0.9). In the crawler check (app/services/crawler-check.server.ts and crawler-info.ts), group agents into three families with the vendor's documented purpose, quoted from the vendor doc and with the URL in a code comment: training (GPTBot, ClaudeBot, Google-Extended, CCBot), search index (OAI-SearchBot, Claude-SearchBot, PerplexityBot, Googlebot, Bingbot), user fetch (ChatGPT-User, Claude-User, Perplexity-User). Report per family: allowed by robots.txt, page returns 200 to that user agent, and whether the response contains `class="ai-visibility-content"`. Keep the existing 5-agent behaviour as a subset, do not remove any agent. Merchant sentence when a training crawler is blocked: "This crawler collects pages that train the model. Blocking it means the model will not learn your products from your own site." Tests per family.

14. Uninstall paragraph (P1.3). SUPPORT.md and PRIVACY.md in _shopify: one paragraph, what stays (attributes, summaries, tables, meta titles and descriptions, alt text: Shopify metafields and fields in the store) and what stops (the visible blocks, the structured data block, the plain-text pages, llms.txt, agents.md: served by the app, gone at uninstall). Also fix the refund answer in SUPPORT.md: do not invent a policy, replace the 14-day sentence with "Billing runs through Shopify. Write to us at hello@mrdigital.ro and we will answer within two working days." and flag in the handover that the refund policy text needs Marius's decision.

15. Theme-file templates as copy text (P1.4). Behind the existing operator key (isSeoUnlocked), a Diagnostics card "Files your developer can add to the theme" showing, as copyable code blocks, `templates/agents.md.liquid` with this exact body:

# {{ agents.store_name }}

> Product data for AI agents at {{ agents.store_url }}. The complete, always current product index is served by the AI Visibility app at the links below.

## Product index
- [llms.txt]({{ agents.store_url }}/apps/ai-visibility/llms.txt): every published product, each with a plain text page
- [agents.md]({{ agents.store_url }}/apps/ai-visibility/agents.md): agent notes for this store

## Commerce Protocol (UCP)
- Discovery: `GET {{ agents.ucp_discovery_url }}`
- MCP endpoint: `POST {{ agents.mcp_endpoint_url }}`
- Sitemap: {{ agents.sitemap_url }}

with the note "Shopify serves /llms.txt from this file too. Paste it in the theme code editor under Templates. Nothing else in the theme changes." The app never writes it. No robots.txt fragment (Shopify's default blocks no AI crawler; verified 11 September 2026).

16. Docs and handover. CHANGELOG.md "Unreleased": one entry per item above, in the house style (what was wrong, what changed, what was measured, tests run with counts). STATUS.md: replace the "Where things stand" section with a dated one for today. Then run `.\check.bat` and `node scripts\check-liquid.mjs` and `node scripts\check-liquid-json.mjs`, and `shopify theme check` on the extension if available. Handover, as the last message, in this exact shape:

- First line: the full-suite result with file and test counts, and what could not be run.
- Table of commits: hash, one line, files touched.
- Table of shared symbols changed: symbol, construction sites found (count), files.
- What is unverified on a real store and needs Marius: the App embeds toggle for "AI Visibility content" on Republica BIO, the nightly scan, `npx tsx scripts/read-ld-visible.ts republicabio.myshopify.com 20`, `npx tsx scripts/read-llms-txt.ts republicabio.myshopify.com`, the Gemini test from the PRD.
- Deploy commands for Marius, in one block, starting with `cd F:\ai-visibility-shopify` and `npx tsx scripts/queue-unstick.ts`, then git push, `npx shopify app deploy`, tag `deploy-2026-09-11-2`, push --tags.
- Everything you decided alone, with the reason, in one list. Everything you could not do, with the reason, in another.
