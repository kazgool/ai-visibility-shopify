Second batch on top of your AI-readability handover, same deploy. Nothing is pushed yet; keep it that way. Same hard rules as _shopify/CC-PROMPT-AI-READABILITY.md, plus the ones below. Marius approved today: the three PRD amendments you proposed (no maxJobExpiry in graphile 0.16.6, manual_placement instead of detection, the wider P0.3 scope), and every item below. Read _shopify/PRD-AI-READABILITY.md again for context, then work in this order, one commit per item.

Why this batch exists: the visible block would print English template sentences on Republica BIO, a Romanian storefront, on 189 pages: summary.ts builds "Key details: ...", "Priced at 26.90 RON from Republica BIO." and questions like "What is X made of?"; the heading, "Suits:" and link texts are English too. And buildQuestions matches furniture labels only (material, dimensions, seats, includes, capacity, room), so on a food catalogue with a 26-group dictionary it produces the same few commerce questions (price, delivery, returns, payment) on every product, one of them with a price frozen into text. Verify both claims on real data first (item 1) before changing anything.

1. Measure before. Run `npx tsx scripts/audit-engine-run.ts` (read its header for arguments) on both catalogues it supports (Republica BIO 189 products, the 355-product furniture fixture). Save to _shopify/audit-logs-2026-09-11/engine-before.md: per catalogue, summary count, question count per product (distribution), the set of question templates fired with counts, how many summaries contain a price sentence, and 10 sample Q&A pairs per catalogue. This is the "before" for item 5.

2. Repo cleanup. `git rm` _shopify/repo-snapshot-080e0d6.tar.gz and ibility-shopify (the second is git CRLF warnings captured by a mistyped redirect, the first a snapshot made for an audit; neither is source). Add both paths plus `_shopify/*.tar.gz` to .gitignore. In SUPPORT.md make the response time match the Contact section, "within one working day", in both places. Do not touch the refund wording further; it stays flagged for Marius.

3. Heartbeat for long jobs. seo_scan_products (worker/tasks.ts) writes no JobRun progress until the end, so a scan past 30 minutes reads "stuck" under job-stale.ts while it is running and the guard lets a second one start. Pass a progress callback into scanProductPagesForShop and write progress (done, total) at most once every 10 pages or 60 seconds, whichever comes first. Then audit every task in worker/tasks.ts that owns a JobRun: for each, state the longest possible gap between JobRun writes (loop sizes, fetch delays, allowances) and add the same throttled heartbeat wherever a gap can exceed 10 minutes. Table in the handover: task, longest gap before, after.

4. Content language setting. Business screen (app/routes/app.business.tsx, business.server.ts, BusinessRecord): a new field `contentLanguage`, values "en" or "ro", label "Language your product pages are written in", help text "The summary and buyer questions this app writes use this language." Detection: the app's scopes are read_products, write_products, read_themes, read_markets, read_online_store_navigation. shopLocales needs read_locales or read_markets_home, which the app does not have; do not add a scope (it forces the merchant to re-approve). Research on shopify.dev whether the shop's primary or default locale is readable with read_markets (for example the markets web presence defaultLocale). If it is, use it as the default when the field is unset, and say which query and doc URL. If it is not, the field stays unset until the merchant chooses, the engine keeps English while unset, and the dashboard shows a step "Choose the language your product pages are written in" until it is set. Unset never breaks anything that works today. Every construction site of BusinessRecord grepped and counted.

5. Engine output in the content language, price out, questions from the merchant's own labels. The engine stays pure: the language comes in on CapsuleInput (and on whatever input meta.ts and the mirror renderer take, if they emit fixed phrases; grep them all).
   a. One phrase table, app/engine/phrases.ts, with en and ro for every fixed phrase the engine emits into merchant-visible or AI-visible text: summary, questions and answers, fit_for sentences, mirror headings, llms.txt "Buying it" lines if they come from the engine. Nothing else in the engine hardcodes a sentence afterwards; a test fails if one appears (grep test on the engine sources for the old English strings outside phrases.ts).
   b. Romanian uses standard diacritics (ă â î ș ț). Check that cleanOutput keeps them and add a test. Plain-character rules still apply (no em dash, no curly quotes, no ellipsis character).
   c. Remove the price sentence from buildSummary and the "How much does X cost?" question from buildQuestions, both languages. Chesterton's fence: the comment "Commercials in the sentence, not only in the markup" explains why it was there; write in the CHANGELOG why it goes (the page and the Product node's offers carry the live price; a price frozen into generated text is wrong at the first sale, e.g. Ashwagandha 81.01 vs 98.80 lei on 11 September). If a WordPress fixture in fixtures.test.ts asserts the price sentence, stop and report it in the handover instead of changing the fixture; the fixtures are a contract.
   d. Questions from the product's own facts. Keep the existing label-specific templates (material, dimensions, seats, includes, capacity, room) where they match. For every other fact label, one generic template: en "What {label} does {title} have?" answer "{value}.", ro "Ce {label} are {title}?" answer "{value}.", with {label} lower-cased as stored in the merchant's dictionary. Order: label-specific first, then generic in the orderFacts order, then the business questions (delivery, returns, warranty, payment). Cap at 6 questions per product. A question is never emitted without a real answer (existing rule). Skip a generic question when its label is one of the dose or instruction groups the engine already treats specially (grep DOSE_MARKERS and the serving context in extract.ts); say which labels you skipped and why.
   e. Measure after: rerun audit-engine-run.ts on both catalogues into engine-after.md with the same metrics as item 1, plus a diff section: questions added and removed per template, summaries changed, and 20 sample Q&A pairs from Republica BIO in Romanian and 10 from the furniture catalogue in English. Read every removed item; list any true fact that disappeared. Zero lost facts is a claim that needs the list behind it.

6. Storefront strings in the extension's locale files. extensions/ai-visibility/locales/en.default.json and ro.json (storefront) and the matching .schema.json files for editor labels: heading defaults ("About this product", "About this collection"), "Suits", "Plain-text version of this page", "All products as plain text", and any other fixed string the snippets print. Use the t filter in the snippets. Heading settings: blank default; when blank, print the translated default; when the merchant typed one, print theirs. Cite the shopify.dev page on theme app extension locales in a comment. Tests render the snippet under both locales (liquid harness) and assert no English fixed string appears in the ro render.

7. Regeneration path, no new write logic. Find the existing pass that recomputes summary and questions for a shop (grep buildSummary and buildQuestions callers; likely bulk_extract). Confirm by reading it that it writes only auto-provenance values and skips identical values. When contentLanguage changes on the Business screen, enqueue that pass for the shop (same guard, same one-job-at-a-time rule), and say so on the screen after save: "Summaries and questions are being rewritten in the new language. Anything you edited yourself is kept." Test: a save that changes the language enqueues one job; a save that does not, enqueues none.

8. Switch-on step. The new embed is off after deploy on every store. Dashboard step "Show this app's content on your product pages" with the theme editor deep link that activates an app embed (look up the documented URL format on shopify.dev, theme app extensions UX page; cite it). The step is done when embed-check sees the "AI Visibility content" embed active; extend embed-check.server.ts to report it separately from the head embed.

9. Docs and handover. CHANGELOG entries per item in house style, STATUS.md updated, PRD amendments marked approved with today's date. Full .\check.bat, check-liquid, check-liquid-json, theme check. Handover in the same shape as last time, plus: the phrase table (en and ro side by side) for Marius to approve as client-facing wording, the heartbeat table, the before/after engine metrics, and the list of labels skipped in 5d. Deploy block unchanged except the tag message.
